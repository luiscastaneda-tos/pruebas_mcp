import express from "express"; import request from "supertest"; import { describe, expect, it } from "vitest"; import { ValidationError, NotFoundError } from "../../src/core/errors/index.js"; import { errorHandler } from "../../src/core/middleware/errorHandler.js";
function app(error: unknown){const a=express();a.get("/",(_q,_s,n)=>n(error));a.use(errorHandler);return a;}
describe("forma estándar de errores",()=>{it("incluye details solo en VALIDATION_ERROR",async()=>{const r=await request(app(new ValidationError("inválido",[{field:"x",issue:"Requerido"}]))).get("/");expect(r.body).toEqual({success:false,error:{code:"VALIDATION_ERROR",message:"inválido",details:[{field:"x",issue:"Requerido"}]}});});it("omite details en NOT_FOUND",async()=>{const r=await request(app(new NotFoundError("Reserva","x"))).get("/");expect(r.body.success).toBe(false);expect(r.body.error.code).toBe("NOT_FOUND");expect(r.body.error.details).toBeUndefined();});});
